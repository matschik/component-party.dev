export component Time() {
  let time = #ripple.track(new Date().toLocaleTimeString());

  #ripple.effect(() => {
    const timer = setInterval(() => {
      @time = new Date().toLocaleTimeString();
    }, 1000);

    return () => clearInterval(timer);
  });

  <h1>{`Current time: ${@time}`}</h1>
}
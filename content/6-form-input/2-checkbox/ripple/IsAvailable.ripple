import { bindChecked } from "ripple";

export component IsAvailable() {
  let isAvailable = #ripple.track(false);

  <input 
    id="is-available" 
    type="checkbox"
    {ref bindChecked(isAvailable)}
  />
  <label for="is-available">{`Is available`}</label>
}